## AI Summary

A file named maxContextCalc.py.


### Function: maxCtxFont(font)

**Description:** Calculate the usMaxContext value for an entire font.

### Function: maxCtxSubtable(maxCtx, tag, lookupType, st)

**Description:** Calculate usMaxContext based on a single lookup table (and an existing
max value).

### Function: maxCtxContextualSubtable(maxCtx, st, ruleType, chain)

**Description:** Calculate usMaxContext based on a contextual feature subtable.

### Function: maxCtxContextualRule(maxCtx, st, chain)

**Description:** Calculate usMaxContext based on a contextual feature rule.
