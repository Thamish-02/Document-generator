## AI Summary

A file named _keywords.py.


### Function: patternProperties(validator, patternProperties, instance, schema)

### Function: propertyNames(validator, propertyNames, instance, schema)

### Function: additionalProperties(validator, aP, instance, schema)

### Function: items(validator, items, instance, schema)

### Function: const(validator, const, instance, schema)

### Function: contains(validator, contains, instance, schema)

### Function: exclusiveMinimum(validator, minimum, instance, schema)

### Function: exclusiveMaximum(validator, maximum, instance, schema)

### Function: minimum(validator, minimum, instance, schema)

### Function: maximum(validator, maximum, instance, schema)

### Function: multipleOf(validator, dB, instance, schema)

### Function: minItems(validator, mI, instance, schema)

### Function: maxItems(validator, mI, instance, schema)

### Function: uniqueItems(validator, uI, instance, schema)

### Function: pattern(validator, patrn, instance, schema)

### Function: format(validator, format, instance, schema)

### Function: minLength(validator, mL, instance, schema)

### Function: maxLength(validator, mL, instance, schema)

### Function: dependentRequired(validator, dependentRequired, instance, schema)

### Function: dependentSchemas(validator, dependentSchemas, instance, schema)

### Function: enum(validator, enums, instance, schema)

### Function: ref(validator, ref, instance, schema)

### Function: dynamicRef(validator, dynamicRef, instance, schema)

### Function: type(validator, types, instance, schema)

### Function: properties(validator, properties, instance, schema)

### Function: required(validator, required, instance, schema)

### Function: minProperties(validator, mP, instance, schema)

### Function: maxProperties(validator, mP, instance, schema)

### Function: allOf(validator, allOf, instance, schema)

### Function: anyOf(validator, anyOf, instance, schema)

### Function: oneOf(validator, oneOf, instance, schema)

### Function: not_(validator, not_schema, instance, schema)

### Function: if_(validator, if_schema, instance, schema)

### Function: unevaluatedItems(validator, unevaluatedItems, instance, schema)

### Function: unevaluatedProperties(validator, unevaluatedProperties, instance, schema)

### Function: prefixItems(validator, prefixItems, instance, schema)
