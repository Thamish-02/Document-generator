## AI Summary

A file named _process_cli.py.


### Function: system(cmd)

**Description:** system(cmd) should work in a cli environment on Mac OSX, Linux,
and Windows

### Function: getoutput(cmd)

**Description:** getoutput(cmd) should work in a cli environment on Mac OSX, Linux,
and Windows

### Function: check_pid(pid)

**Description:** Check if a process with the given PID (pid) exists
